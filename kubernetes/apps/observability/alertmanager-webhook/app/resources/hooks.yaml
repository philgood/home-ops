---
# Webhook hooks configuration for AlertManager → Home Assistant
- id: "alertmanager"
  execute-command: "/bin/sh"
  command-working-directory: "/tmp"
  pass-arguments-to-command:
    - source: "string"
      name: "-c"
    - source: "string"
      name: |
        PAYLOAD="$1"
        HA_URL="https://hass.narden.au/api/webhook/k8s_temp_a7f3c9e2b4d1a"
        echo "$PAYLOAD" | wget -q -O- --header="Content-Type: application/json" --post-data="$PAYLOAD" "$HA_URL" 2>&1 || echo "Failed" >&2
        echo "Forwarded to HA"
    - source: "entire-payload"
  response-message: "Alert forwarded to Home Assistant"