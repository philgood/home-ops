---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-api-server
  annotations:
    # renovate: depName="Kubernetes / System / API Server"
    grafana.integreatly.org/dashboard-revision: "20"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 15761
    revision: 20
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-coredns
  annotations:
    # renovate: depName="Kubernetes / System / CoreDNS"
    grafana.integreatly.org/dashboard-revision: "22"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 15762
    revision: 22
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-global
  annotations:
    # renovate: depName="Kubernetes / Views / Global"
    grafana.integreatly.org/dashboard-revision: "43"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 15757
    revision: 43
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-namespaces
  annotations:
    # renovate: depName="Kubernetes / Views / Namespaces"
    grafana.integreatly.org/dashboard-revision: "44"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 15758
    revision: 44
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-nodes
  annotations:
    # renovate: depName="Kubernetes / Views / Nodes"
    grafana.integreatly.org/dashboard-revision: "40"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 15759
    revision: 40
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-pods
  annotations:
    # renovate: depName="Kubernetes / Views / Pods"
    grafana.integreatly.org/dashboard-revision: "37"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 15760
    revision: 37
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: kubernetes-volumes
  annotations:
    # renovate: depName="K8s / Storage / Volumes / Cluster"
    grafana.integreatly.org/dashboard-revision: "14"
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  datasources:
    - datasourceName: Prometheus
      inputName: DS_PROMETHEUS
  folder: Kubernetes
  grafanaCom:
    id: 11454
    revision: 14
