---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/tuppr.home-operations.com/kubernetesupgrade_v1alpha1.json
apiVersion: tuppr.home-operations.com/v1alpha1
kind: KubernetesUpgrade
metadata:
  name: kubernetes
spec:
  kubernetes:
    # Must stay within what the running Talos supports: the whole 1.13 line
    # tops out at Kubernetes 1.36.3, so 1.37 needs Talos 1.14 first. Keep this
    # in step with kubernetesVersion in talos/talenv.yaml -- when they disagree,
    # tuppr and the generated machine config fight over the cluster.
    # renovate: datasource=docker depName=ghcr.io/siderolabs/kubelet
    version: v1.36.3
  healthChecks:
    - apiVersion: volsync.backube/v1alpha1
      kind: ReplicationSource
      expr: |-
        status.conditions.filter(c, c.type == "Synchronizing").all(c, c.status == "False")
    - apiVersion: ceph.rook.io/v1
      kind: CephCluster
      expr: |-
        status.ceph.health in ['HEALTH_OK']
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      expr: |-
        status.conditions.filter(c, c.type == "Ready").all(c, c.status == "True")
    - apiVersion: kustomize.toolkit.fluxcd.io/v1
      kind: Kustomization
      expr: |-
        status.conditions.filter(c, c.type == "Ready").all(c, c.status == "True")
