---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tesla-proxy
spec:
  secretName: tesla-proxy-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: tesla.narden.au
  dnsNames:
    - tesla.narden.au
  privateKey:
    rotationPolicy: Always